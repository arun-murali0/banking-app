'use server';

// new User
export const newUser = async () => {
	try {
	} catch (error) {
		console.error(error);
	}
};


// user login
export const loginUser = async () => {
	try {
	} catch (error) {
		console.error(error);
	}
};
